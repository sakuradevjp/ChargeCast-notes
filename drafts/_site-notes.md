# Site build notes

## Theme
- **Default: light theme** (no JS theme switcher)
- Respect `prefers-color-scheme` via CSS media query only — users toggle at OS/browser level

## Language switcher
- Keep minimal (just a dropdown / link list in header/footer)
- Default landing: `/` → English, with links to `/ja/`, `/zh-Hans/`, etc.
